

//this should really be a database (well, the whole thing should be a database)
var manifest = []; //array of repos sorted by access time (newest ---> oldest)

module.exports.access = function(user, repo) {
	
};
